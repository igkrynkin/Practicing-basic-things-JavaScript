/* document.write('Hello world!');
         write - выводит на прямую в документ. */
         
         console.log('Hello world!');
         // console - встроенный объект к которому мы обращаемся, а внутри этого объекта существуют некоторые функции, как например внутри объекта document существовала функция write, так и внутри объекта console существует функция log к которой мы можем обратиться 